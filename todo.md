> when you select a link on the top overlaw it does move the content vertically but the position of the elmetns is bit up ,   I belive the reason is that you start displayign the overlay when the user scrolls up & when the first category is almost over scrolled,  we wnat you to show the overlay when the first category touches the top of the screen. 



> great on overlay cut the space between restaurnat name text and menu options  by  25%

> Update padding/margin between restaurant name and menu items to reduce vertical spacing by 25%